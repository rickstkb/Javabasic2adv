package inter.aula53.enumeradores;

// A diferenca e que quando se declara enums, 
public enum DiaSemana {
	
	// Segue padrao de nomenclatura de constantes
	// Aqui criou-se um enum representando cada dia da semana
	// Enums pode ser tratados como um tipo/como se tivesse utilizando uma classe dia da semana 
	
	// Garante que os valores atribuidos as variaveis que ja sao possuem o proprio valor serao mantidas fixas
	SEG, TER, QUA, QUI, SEX, SAB, DOM; 
}
