package inter.aula73.thread_resume_suspend_stop;

// Criando metodos para suspender/pausar, resumir e parar a execucao de uma thread
// Existiam ate o java 2 na classe thread, porem criavam problemas e foram removidos
// Suspende criava deadlock, resume nao funciona sem suspende, e stop tbm (hoje utilizado interrupt)

// Por mais que nao existam mais sao uteis, e podem ser implementados manualmente, fazendo uso de flags e demais recursos
// wait, notify, syncronized
// met suspend, pausa temporariamente execucao
// resume continua
// finalizar exec stop
public class Def {

}
